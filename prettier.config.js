const config = {
    semi: true,
    trailingComma: 'all', // e.g., es5 or none
    singleQuote: true,
    printWidth: 120,
    tabWidth: 4,
    arrowParens: 'always', // e.g., 'avoid'
};

export default config;
